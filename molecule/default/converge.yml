---

- name: Converge
  hosts: all
  become: "yes"
  vars:
    chocolatey_packages: ['putty.install', {'name': 'mobaxterm', 'ignore_checksums': True}]
  tasks:
    - name: Include role 'ansible-role-chocolatey'
      ansible.builtin.include_role:
        name: ansible-role-chocolatey
